【過去データ学習(較正)エンジンを追加した版】
新規：
 - /api/train … サーバー側で過去レースを集めて「1着確率モデル」を学習し、
   学習済み重み・較正表・推奨しきい値を返す（購入/見送りの精度較正用）。
 - numpy を依存に追加。
アップロード：
 1. GitHub の boat-race-ai に app.py と requirements.txt を上げて commit（上書き）
 2. Render → Manual Deploy → Deploy latest commit
 3. 「Application startup complete」を確認
デプロイ後、こちらで /api/train を実行して学習し、結果の重みをアプリに反映します。
