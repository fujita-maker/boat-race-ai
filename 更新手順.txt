【学習結果を“画面判定”に組み込んだ最終版】
 - 頭(1着)の判定＝過去4530レースで学習した1着確率モデルに置換。
 - 1着信頼度は「1号艇の1着確率○%」で表示。60%未満は自動で「見送り推奨」（赤）。
 - これが噛み合っていれば、信頼度に必ず「○%」の数字が出ます（出ない＝古い版）。
アップロード：app.py を上書き → Render → Manual Deploy → Deploy latest commit。
